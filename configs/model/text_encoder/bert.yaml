# _target_: csref.model.text_encoder.Bert
_target_: csref.modeling.text_encoder.Bert
#pretrained_path: "data/weights/bert-large-uncased"
pretrained_path: "data/weights/bert-base-uncased"
freeze_model: True
hidden_state_index: -1
gradient_checkpointing: False # set to True to save memory at the cost of compute
attn_implementation: null # set to eager | sdpa | flash_attention_2; null uses transformers default
torch_dtype: null # set to bf16 | fp16 | fp32; null/auto follows transformers default
